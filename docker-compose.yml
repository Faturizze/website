services:
  faturize_web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    networks:
      - faturize_web_network

networks:
  faturize_web_network:
    driver: bridge
      